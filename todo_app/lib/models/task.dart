class Task {
  String name;
  String description;
  DateTime dueDate; // Ubah menjadi DateTime

  Task({
    required this.name,
    required this.description,
    required this.dueDate, // Ubah menjadi DateTime
  });
}
